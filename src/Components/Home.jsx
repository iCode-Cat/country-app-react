import React, { useState } from "react";

import Flags from "../Components/Flags";
const Home = () => {
  return (
    <div className="flags-container">
      <Flags />
    </div>
  );
};

export default Home;
